import NotFoundPage from '@/components/NotFoundPage';

// There goes all requests caught by catch-all
// only routse with proper locale prefix are allowed.
export default function NotFound() {
  return <NotFoundPage />;
}
